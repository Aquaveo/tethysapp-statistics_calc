version: 2.0.0
name: statistics_calc

requirements:
  conda:
    channels:
      - conda-forge
    packages:
      - django
      - pandas
      - numpy
      - requests
      - pytz
      - scipy
      - scikit-learn
      - hydrostats
